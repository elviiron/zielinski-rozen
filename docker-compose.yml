version: '3'
services:
  app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    depends_on:
      - "db"

  db:
    image:  keinos/sqlite3:latest
    volumes:
      - ./product_parf.db:/product_parf.db
  
      
